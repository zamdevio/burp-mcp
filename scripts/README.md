# Scripts

Optional helpers for maintainers. **Build and test stay on Gradle:**

```bash
./gradlew test
./gradlew embedProxyJar
```

Add shell scripts here when needed (release checks, local smoke checklists). Do not duplicate Gradle tasks without reason.

See **`maintainer/systems/health.md`** for gates.
